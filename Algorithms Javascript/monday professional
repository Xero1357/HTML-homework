var A = 100;

while (A > 1) {
    let B; 

    if (A % 3 === 0) {
        B = A / 3;
    } else if ((A + 1) % 3 === 0) {
        B = (A + 1) / 3;
    } else if ((A - 1) % 3 === 0) {
        B = (A - 1) / 3;
    }

    A = B; 
    console.log(B)
}
