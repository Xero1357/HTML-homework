
function pow(base, exponent) {
  let result = 1;
  for (let i = 0; i < exponent; i++) {
      result *= base;
  }
  return result;
}

let x = [3, 3, 1];
let n = [2, 3, 100];
let results = [];

for (let i = 0; i < x.length; i++) {
  results.push(pow(x[i], n[i]));
}

console.log(results);
